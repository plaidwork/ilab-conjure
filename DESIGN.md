---
version: "1.0"
name: "iLab CONJURE Design System"
description: "Design standards for the local image-generation workbench."
tokens:
  colors:
    background: "#F3F6F4"
    surface: "#FFFFFF"
    surfaceSoft: "#F8FAF9"
    line: "#E2E8E5"
    text: "#1F352F"
    textSecondary: "#4A635B"
    muted: "#889D95"
    primary: "#457B66"
    primaryForeground: "#FFFFFF"
    primaryStrong: "#335D4D"
    primaryLight: "#E6F0EC"
    accent: "#9B7F4F"
    danger: "#A6463D"
    statusBlue: "#2F6FE4"
    code: "#142F2D"
    codeText: "#BFE0CF"
    successSoft: "#EEF6F2"
    dangerSoft: "#FFF4F2"
    darkBackground: "#101815"
    darkSurface: "#17221E"
    darkSurfaceSoft: "#1E2B26"
    darkLine: "#31433B"
    darkText: "#E7F0EC"
    darkTextSecondary: "#B9CAC2"
    darkMuted: "#82978E"
    darkPrimary: "#72B79B"
    darkPrimaryForeground: "#10201B"
    darkPrimaryStrong: "#A6D8C5"
    darkPrimaryLight: "#243D34"
    darkAccent: "#D3B776"
    darkDanger: "#E18479"
    darkStatusBlue: "#83A9FF"
    darkCode: "#0B1110"
    darkCodeText: "#C9E8D9"
    darkSuccessSoft: "#1B342B"
    darkDangerSoft: "#3A201D"
  typography:
    fontFamily: "Inter, PingFang SC, Hiragino Sans GB, Microsoft YaHei, sans-serif"
    bodySize: "14px"
    bodyLineHeight: "1.45"
    panelTitleSize: "15px"
    modalTitleSize: "17px"
    labelSize: "13px"
    helperSize: "12px"
    tinySize: "11px"
    letterSpacing: "0"
  spacing:
    xxs: "3px"
    xs: "6px"
    sm: "8px"
    md: "12px"
    lg: "16px"
    xl: "20px"
    xxl: "30px"
    panelPadding: "20px"
    modalPadding: "20px"
    gridGap: "16px"
    denseGap: "8px"
  rounded:
    small: "6px"
    base: "8px"
    large: "12px"
    modal: "18px"
    pill: "999px"
  shadows:
    small: "0 2px 8px rgba(29, 52, 46, 0.04)"
    popover: "0 14px 34px rgba(29, 52, 46, 0.16)"
    modal: "0 24px 80px rgba(24, 45, 39, 0.24)"
  motion:
    fast: "140ms ease"
    base: "180ms ease"
    height: "220ms ease"
    transform: "180ms ease"
  components:
    panel:
      background: "surface"
      border: "line"
      radius: "large"
      padding: "panelPadding"
      shadow: "small"
    modal:
      background: "surface"
      radius: "modal"
      shadow: "modal"
      padding: "modalPadding"
    primaryButton:
      background: "primary"
      foreground: "primaryForeground"
      radius: "base"
      height: "44px"
    runButton:
      background: "primary"
      foreground: "primaryForeground"
      radius: "pill"
      height: "36px"
    ghostButton:
      background: "surfaceSoft"
      foreground: "textSecondary"
      border: "line"
      radius: "base"
    input:
      background: "surface"
      foreground: "text"
      border: "line"
      radius: "base"
      height: "36px"
    segmentedControl:
      background: "surfaceSoft"
      activeBackground: "primary"
      activeForeground: "primaryForeground"
      radius: "base"
    galleryCard:
      background: "surfaceSoft"
      border: "line"
      radius: "large"
---

# iLab CONJURE Design System

## Overview

iLab CONJURE 是本地图片生成工作台。界面目标是高频操作、参数配置、队列查看、素材管理和结果预览，不是营销页或内容展示站。

新增功能应该优先融入现有工作台结构：左侧导航、主操作区、预览区、弹窗图库、任务队列。视觉基调保持安静、清晰、密集但不拥挤，让用户可以快速扫描状态并完成生成任务。

设计来源以本文件 tokens、`codex_image/webui/static/styles/` 源分片和生成后的 CSS 变量为准。新增组件优先复用已有类名和交互模式，再补充小范围样式；不要直接把长期维护样式堆进生成后的 `static/styles.css`。

WebUI 已从单一前端脚本逐步拆成 TypeScript 功能模块。设计新增或调整时，要同时考虑视觉结果和实现边界：组件外观、状态文案、事件入口、数据模型和持久化接口应落在对应模块里，避免为了一个界面改动把渲染、状态、请求和样式继续堆回单个大文件。

## Brand And Icons

项目主标识使用简单、可爱、可识别的小兔子图形。WebUI 左上角主 Logo、macOS `.app` 图标、Windows 程序图标和托盘 / 菜单栏小图标应保持同一轮廓来源，优先使用实色填充兔子而不是细描边图形，保证在 16px 到 32px 的系统状态栏尺寸下仍能辨认。

品牌源文件保留为 SVG，便于生成多尺寸位图和 `.icns` / `.ico`。应用图标可以使用品牌主色底和白色兔子；macOS 菜单栏和 Windows 托盘小图标应使用系统可识别的 template / 单色版本，不带白色方形背景，也不要把图形放得过小或偏上。状态栏图标需要占满可用画布的主要高度，保留下方视觉平衡。

独立历史库必须复用生成页相同的小兔子 SVG，并保持 Logo 尺寸和轮廓不变；历史库本身是侧栏主标题，不再重复显示 `iLab CONJURE` 产品名。Logo 继续链接生成页，标题区下方另提供明确的“返回生成页”入口。该入口属于高频页面切换，使用侧栏满宽、内容居中、`44px` 高的浅色次级按钮，不使用实心主色冒充提交动作；返回箭头、清晰焦点环和完整可访问名称必须保留。历史库顶部复用生成页完整工具条，包括供应商、队列、任务通知、主题、GitHub 和系统设置；主题切换只能留在这条共享工具条中，不能混入历史库的视图、排序和刷新控件。主题恢复脚本必须在样式表之前运行，避免页面首帧闪成错误主题。

## Colors

主色是低饱和墨绿色，用于主要操作、选中态、关键状态和可点击强调。中性色承担页面大面积背景、面板、边框和说明文字。

- `background` 用于页面底色和工作区背景。
- `surface` 用于面板、弹窗、输入区域和卡片主体。
- `surfaceSoft` 用于弱分组、按钮底色、图库卡片和列表行。
- `line` 用于边框和分隔线，优先用边框建立层级。
- `primary` 和 `primaryStrong` 用于主按钮、选中态、链接式强调；填充态文字必须使用 `primaryForeground`，深色模式不能沿用白字。
- `primaryLight` 用于轻量选中背景、提示块和 hover 背景。
- `accent` 只用于少量次级强调，不要扩展成大面积主题色。
- `danger` 只用于删除、清空、覆盖等破坏性操作。
- `statusBlue` 用于队列、进度或外部 API 状态，不替代主色。

深色模式沿用浅色模式的语义角色。除非现有 token 无法表达状态，不要给组件单独写一套深色专用颜色。优先使用 `--surface`、`--surface-soft`、`--line`、`--text`、`--text-secondary`、`--primary-light`、`--success-soft` 和 `--danger-soft`。

深色模式中面板边框、模块标题和历史任务标题要降低视觉权重：面板描边使用 `--panel-border`，模块标题使用 `--section-heading`，历史任务标题使用 `--task-title`。不要在深色面板上使用浅色模式的白色描边或满强度正文色作为默认标题色。

新增颜色必须先判断是否能由现有 tokens 表达。确实需要新增时，补充到本文件和 CSS 变量，并说明用途。

## Typography

默认字体栈是 `Inter`, `PingFang SC`, `Hiragino Sans GB`, `Microsoft YaHei`, `sans-serif`。中文界面要优先保证清晰扫描，不追求大标题冲击力。

- 正文默认 `14px`，行高约 `1.45`。
- 面板标题使用 `15px`、`600`，保持紧凑。
- 弹窗标题可用 `17px`，不要做 hero 级标题。
- 表单标签、帮助文本、元信息分别使用 `13px`、`12px`、`11px`。
- 字间距保持 `0`，不要使用负字距。
- 不用视口宽度动态缩放字体。
- 按钮和标签文字要短，避免在窄屏或中文长词下挤压换行。

界面文案优先描述对象和动作，例如“使用”“分类”“备注”“删除”。不要在页面中写大段功能说明或教程。

## Layout

桌面端保持工作台布局：左侧边栏、主操作列、预览列。左侧边栏宽度在 `280px` 到 `520px` 内调整，默认约 `347px`。生成工作区沿用稳定的原始几何：dashboard 最大宽度 `1760px` 并居中，常规桌面主操作列最大宽度约 `760px`，预览列吸收 dashboard 内剩余空间；真实工作区超过 `1760px` 时不继续放大表单，额外宽度留在 dashboard 两侧。主操作列与预览列由同一个 CSS Grid 行对齐，不使用 JavaScript 测量或同步列高；未锁定的输出设置保持自然高度，额外纵向空间只分配给参考区和提示词区，避免参数表单内部出现大片空白。不要让锁定功能改写 dashboard、控制列或预览列的几何。

页面区域之间使用 `16px` 到 `20px` 间距。紧凑控件组使用 `8px` 到 `12px` 间距。面板内边距默认 `20px`，复杂设置区可以用 `12px` 的内部网格。

响应式布局分为两个独立权威：视口宽度只决定外壳，`1180px` 及以下把垂直侧栏转换为单行紧凑顶栏；主操作区与预览区由 `.main-wrapper` 的 `workspace` 容器宽度决定，容器宽度大于 `1100px` 使用宽松双栏，`900–1100px` 使用可收缩的紧凑双栏，小于 `900px` 才上下堆叠。顶部工具组属于外壳，不属于工作区内容：视口大于 `1180px`、垂直侧栏仍存在时必须贴右排列，不能因工作区进入紧凑容器档而跑到中间。`1024×768` 在紧凑顶栏后仍应保持双栏，`820×1180` 自然进入单栏；侧栏在最小、默认和最大宽度之间变化时也必须按剩余工作区自动重排，不能用浏览器总宽度猜测。新增组件必须检查窄屏横向溢出、文字压缩、固定高度遮挡和预览可达性。

窄屏单栏中的参考输入面板按内容自然撑高；上传区与公用图库上下排列时，面板必须包含两行内容和操作按钮，不能继续套用桌面的单行高度。手机宽度下，紧凑顶栏为状态文本保留空间，新建按钮可收为带可访问名称的图标；主模型或 API 直连说明、联网搜索和提示词处理依次排列。自定义像素与比例输入框随剩余宽度收缩，标签和操作按钮保持可见，不通过裁切或隐藏控件处理溢出。

`1390px` 以下高度的双栏工作区进入连续紧凑密度：通过 `clamp()` 降低面板 padding、面板间距、上传区、提示词区和参数控件高度，不在 `860px` 或其他单一高度突然切换整套轨道。任何档位都让“参考输入（可选）”“提示词”“输出设置”三个板块标题可见并保持同一左侧阅读轴，计数与锁按钮留在标题行右侧。极矮双栏继续缩短参考区并保证最近上传、分类和管理按钮互不覆盖，提示词保留可用输入高度，输出设置完整落在列底部；参考区和提示词区按较轻、较重的弹性比例吸收剩余高度，不能在 `770–860px` 区间留下列底空洞。dashboard 是工作区页面级纵向滚动所有者，控制列不新增内部页面级滚动；预览列在同一网格行内缩放图片。单栏时预览排在控制列之后且空态、单图、多图都至少保留 `260px` 块尺寸。长提示词继续在编辑器内部滚动，图像输入保留可操作的最小高度；短屏且工作区仍处于紧凑双栏时继续保留图像输入左上传区 / 右公用图库侧栏结构。不得让 dashboard、控制列和 body 同时竞争页面滚动，也不得把输出设置或预览画出工作区边界。

控制列的三个板块标题和输出设置字段标签在共同左侧阅读轴上使用 `2px` 的逻辑方向光学内缩；控件外框、面板内距和网格边界保持原位，不能用 margin 或 transform 把整组内容推移。

弹窗居中显示，最大高度不超过视口，内部滚动。右侧抽屉用于任务详情、设置详情、资源管理或长列表筛选。不要把页面段落做成浮动大卡片，也不要在卡片里再嵌套卡片。

右侧抽屉内的分区应按内容自然衔接，不能用固定比例制造空白。抽屉标题区和状态摘要只占实际内容高度，长列表区域承担剩余滚动空间并设置最大高度防止溢出。

## Elevation & Depth

层级优先靠背景、边框、间距和状态色表达，阴影只做轻量辅助。

- 普通面板使用 `shadows.small`，避免漂浮感过强。
- 弹窗使用半透明遮罩、轻微 blur 和 `shadows.modal`。
- 下拉、压缩设置、轻量菜单使用 `shadows.popover`。
- 列表、图库、队列行优先用边框和浅底色区分，不要增加重阴影。

新增覆盖层必须有明确关闭路径，并避免遮住关键状态反馈。

## Shapes

基础圆角是 `8px`，标准横向控件和面板使用 `12px`，品牌标识等强调容器使用 `16px`，弹窗使用 `18px`。标签、真正的圆形按钮、运行按钮和状态胶囊可以使用 `999px`；普通输入框不得仅为了柔和感做成胶囊。生成页侧栏中，批量管理等小控件使用 `8px`，模型家族切换器、搜索框、新建按钮和日期分组使用 `12px`，模型切换器内层按外层 padding 内收为 `9px`，主 Logo 使用 `16px`。

控件尺寸要稳定，避免 hover、加载文本、不同分类数量、不同模式切换导致布局跳动。固定格式组件如图库网格、任务行、按钮组、分段控件，应使用明确的高度、宽度、网格轨道或 `aspect-ratio`。

不要为了柔和感扩大所有圆角。工具型界面应保持明确边界。

## Components

### Panels

面板用于承载一个明确任务：参数、提示词、图片输入、预览、队列、图库管理。默认使用 `surface`、`line`、`large` 圆角、`20px` 内边距和轻阴影。

面板标题短小直接。面板内的辅助说明只解释必要后果，例如“分类会保存到图库；提示词用途用于生成参考图说明”。

### Buttons

主按钮用于提交、保存、确认。运行按钮使用 pill 形态，位置稳定。次级动作使用 ghost 按钮。删除、覆盖等高风险动作必须使用危险色，并在批量或不可逆场景增加确认。

按钮文字优先动词，不要把说明塞进按钮。图标按钮必须有可访问名称或 tooltip。提示词和图像输入里的轻量操作优先使用 SVG 图标 + 短动词，例如“清空”“粘贴”；清空类轻量动作默认弱色，hover/focus 时再显出危险色，避免抢走主流程注意力。

图像输入编辑面板默认选中裁剪工具，降低误画风险。画笔、箭头和油漆桶属于“编辑指示标记”工具；只有用户实际用这些工具留下当前可见标记时，保存后才自动补充提示词中的标记说明。单纯裁剪不应让用户的提示词产生隐性附加要求。

编辑面板的图层区要服务识别和选择，而不是只表达顺序。每个图层行必须显示该图层的真实缩略图，选中态通过整行主色和文字反差表达；只有缩略图生成失败时才用序号兜底。多图组合、局部擦除、缩放和旋转属于图像合成动作，不等同于“编辑指示标记”。图片变换默认锁定长宽比例，Shift 才进入自由变换，符合常见桌面图像编辑习惯。

关闭按钮在抽屉、弹窗和管理面板中必须保持同一套视觉和命中体验：使用统一的 X 图标按钮，图标在按钮内居中，点击区域不能小于常规 ghost button，必须提供 `aria-label`。可关闭覆盖层同时支持鼠标关闭和 `Esc` 关闭。

### Segmented Controls

模式切换、API 模式、视图切换使用分段控件。选中态使用 `primary` 背景和 `surface` 文字，非选中态使用 `surfaceSoft`。一维分段控件应使用共享滑动选中底板表达空间连续性，动效对齐 `motion.base`，并支持 `prefers-reduced-motion`。分段控件不能改变父容器宽度。

顶部导航里的可操作控件属于轻量工具条，不是主操作按钮。任务通知、队列、授权来源、主题、GitHub 链接和系统设置入口应同高同圆角，但外层高度保持约 `36px`，内部选项约 `30px`，边框、底色和阴影都要弱化，避免顶栏比主工作区更抢视觉重量。任务通知使用铃铛图标和内收状态点表达“有未读”，不要在铃铛右上显示数字角标，也不要用负偏移贴出控件边界；精确未读数量放在按钮 `aria-label`/tooltip 和通知面板标题里，避免窄屏或贴边时被裁切，同时降低顶栏视觉噪音；弹层只显示最近完成、失败或部分失败任务，缩略图用于快速识别，点击直接切换任务；系统通知开关放在系统设置的 `存储与通知` Tab，默认关闭，不在顶栏请求权限。授权来源只保留 `Codex` 和 `API` 两段；`API` 按钮只负责切换渠道，右侧独立齿轮按钮负责打开系统设置并默认进入 `API 设置` Tab。API 供应商选择不要在顶栏新增常驻或浮层控件；当前生效的 API 供应商名应进入授权来源左侧状态文案，直连状态的信息块里提供明确的 `系统设置`动作并进入 API 设置。生成页供应商菜单将 `Codex Image` 与 `Codex Responses` 作为两个具名的内置绑定，以专用通道标识辅助识别，选择结果必须随请求绑定；系统设置保留 `API 设置`、`网络`、`语言 / Language`、`存储与通知` 四个 Tab。GitHub 图标链接放在主题切换按钮右侧，保持图标按钮形态和外链可访问属性。

### Forms

输入框、下拉、数字控件和文本域使用 `surface` 背景、`line` 边框、`base` 圆角。标签在上，说明文字在下。错误和警告靠状态文本表达，不要只依赖颜色。

参数较多时使用两列或小网格，但每组只承载一个概念。不要把无关设置塞进同一个表单行。

输出设置标题右侧提供单一锁图标。锁定摘要是原参数网格的绝对覆盖层：原参数网格始终参与布局并作为唯一高度来源，锁定时只将网格设为 `visibility: hidden` 与 `inert`，摘要使用 `position: absolute; inset: 0` 覆盖同一面积；禁止读取 DOM 几何、写入同步高度变量或为了锁定态改写 dashboard、控制列和预览列的布局。这样锁定与解锁不会改变输出设置、两列底边或页面滚动模型。摘要内部用一块可快速浏览的只读信息替换表单。GPT Image 2 沿用当前合同：Image 通道不在图形卡上方重复显示由系统设置决定的图像模型，Responses 通道只保留一行居中的轻量“主模型 · 模型名”；三张主卡为画幅、输出张数和文件格式，底部四等分在 Image 显示提示词处理、质量、调用方式和审核，在 Responses 显示提示词处理、质量、搜索和审核。Gemini 必须改由 canonical 模型快照渲染：三张主卡为画幅、输出张数和分辨率；因同系列有多个型号而在上方安静显示具体型号，但不再带“具体型号”标签，查看历史任务时保留任务具体型号。Gemini 底部只显示该型号清单中真实存在的安全设置和 Google 搜索，Lite 不显示搜索，也不能补造文件格式、提示词处理、质量或审核。各型号都使用次级文字色和中等字重，不能压过主卡；卡片标题保留左上定位锚点，图形居中，像素、张数和文件格式等辅助值统一放在图形下方居中并保持同一基线，不使用左上到右下的对角线阅读路径。画幅框、输出张数缩略卡、格式框和分辨率框属于同一信息层级，统一使用同宽单像素描边和同一主色混合强度，不能通过左右加粗或提高饱和度制造无意义的权重差。比例和格式名称只在图形内部出现一次；画幅比例文字只保留字形本身，不使用底色、圆角、内边距或阴影，浅色和深色主题保持一致。输出张数必须绘制与真实数量一致的 1–4 张实体小卡，每张小卡同步当前画幅比例并在极端横竖比例下完整缩放，不能用少一层的堆叠影子代替，也不能裁切或变形。四张且画幅达到 `16:9` 或更宽时，缩略卡切换为居中的 `2 × 2` 网格并适度放大；一至三张或较窄画幅继续单行排列，避免竖幅摘要过高。可见的模型元信息、图形卡和参数摘要作为一个整体在原网格提供的面积内视觉居中，并略偏上保持稳定重心；极矮视口只压缩摘要内部间距和图形尺寸，不改变覆盖层或原网格的外部高度。图形卡与非空参数摘要之间使用中间清晰、两端渐隐的单像素分隔线，不加发光或高对比装饰。当前摘要不重复显示“当前锁定”，锁定状态已由标题栏锁图标表达；查看历史任务时独立保留“所选任务参数”。摘要标题和正文继续使用系统无衬线字体，不引入与工作台不一致的衬线标题。锁定状态只固定生成参数，不新增 Responses / Image 切换字段；当前摘要从当前供应商 binding 派生。选择历史任务时只临时展示该任务的只读参数，不覆盖当前锁定值；只有明确点击“使用此任务参数”才采纳，说明文字必须明确该动作不会改变当前供应商 binding，且不得联动修改授权来源、供应商或绑定。新建任务保留锁定值，解锁后回到同一组可编辑控件。

尺寸模式保持独立整行。预设模式下方用两列承载方向和分辨率，自定义模式下方用一个固定高度的轻量尺寸编辑区承载像素尺寸和可选比例锁定；质量与数量默认同一行，质量在左、数量在右，两者各占 50%，短屏布局下数量可以移到尺寸模式右侧，质量保留在比例右侧，让每行左右控件对齐。预设比例按方图到长图排列：`1:1` 后接 `4:5 / 5:4`、`3:4 / 4:3`、`2:3 / 3:2`、`9:16 / 16:9`、`9:21 / 21:9`。每个比例按钮使用按真实宽高比等比绘制的内联 SVG 轮廓配合文字识别，轮廓跟随当前文字色；极端比例保留可见最短边但不改变方向。预设值切换到自定义输入时，应继承当前可见预设的数值，避免用户重新抄写。自定义宽高只保留一处单位提示，像素尺寸是主操作，比例锁定是辅助约束，不要把它们做成两个同等权重的大卡片。像素宽高输入使用紧凑宽度，交换按钮放在两个像素输入组中间；第一列标签与标题和说明左对齐形成阅读轴，第二个维度标签贴近自己的输入框。比例输入使用更小的单字符数字框，留空代表自由宽高，填满后才同步约束；“取首图”按钮放在比例输入行，并与宽高数字控件垂直居中。自定义模式外层高度必须等于预设模式下“方向/分辨率 + 比例”的组合高度，避免切换尺寸模式时输出设置大面板明显跳动；内部内容保持紧凑成组，不用上中下强行拉散。禁用的取首图动作默认隐藏，避免形成不可用的视觉噪音。内部使用 `宽 : 高` 两个单字符数字输入，只接受 1-9，两位都有效时才按比例同步另一边尺寸，清空任一位回到自由宽高。自定义像素尺寸必须符合 `gpt-image-2` 当前约束：宽高 16-3840、16 的倍数、长短边不超过 3:1、总像素 655,360-8,294,400。图像输入包含可预览参考图时，宽高比可从第一张参考图读取；读到的比例先约分，超过单数字范围时取 1-9 内最接近的比例。交换宽高时同步交换比例位。按钮式选项、隐藏输入和实际提交参数必须保持同步；历史任务恢复、模式切换和自定义尺寸切换都不能让 UI 选中态与提交值分叉。

GPT Image 2 的提示词控制统一称为“提示词处理”，使用“原文 / 保真 / 自动”三个短标签。新表单及未锁定的表单重置默认选择“自动”；恢复已锁定设置或历史任务时继续使用保存的模式。“自动”表示按当前通道默认方式处理，不承诺 Images 直连会额外创意扩写。标题右侧使用轻量问号按钮承载完整说明，悬停、键盘聚焦和点击均可查看，并根据当前 Responses 或 Images 链路切换三种处理方式的真实语义；问号按钮悬停或展开时只改变图标颜色，不绘制填充底色、方形阴影或装饰外框。说明使用不参与文档流的浮层，不能新增常驻说明行或改变输出设置面板高度。点击问号只固定或关闭说明浮层，第一阶段不提供自定义规则编辑入口。

GPT Image 2 的 API 直连不使用主模型，但提示词处理仍参与本次请求：原文按用户输入发送，保真把保真规则随 `prompt` 发送，自动不附加保真规则并保留图库引用等必要说明。直连状态应保留提示词处理控件在原位置，用紧凑信息块替换主模型控件位置来说明图像生成模型来自系统设置的 API 设置、主模型不参与本次请求，并提供进入系统设置 API Tab 的显式按钮；不要把 API 说明作为额外行插到提示词处理下方，也不要让授权来源切换触发输出设置面板高度动画或缩放闪烁。

### Image Input And Gallery

图片输入是核心工作流。上传区、缩略图、图库卡片和参考图提示词必须保持统一结构：图片预览、名称、分类或用途、主要动作、次要动作。

“参考输入（可选）”标题直接说明该区域不是生成必填项，不额外增加常驻说明行。参考输入中的非图片文件只在 Responses 路径可用，必须与 `state.images`、图片图库和灯箱的数据与行为分离；切换到 Images 路径时允许继续保留文件，只有点击生成时才通过失败预览提示切换到 Responses，不能静默忽略或提前清空。

统一上传区的 `dragenter` / `dragover` 阶段只判断拖入内容是否包含文件并允许 `copy`。浏览器可能在悬停阶段保护文件名、扩展名和 `File` 对象，因此格式、扩展名以及 Responses 兼容性校验必须推迟到实际 `drop` 后执行，不能因悬停时读不到完整文件信息而把 `dropEffect` 设为 `none`。

已选图片与文件共用同一条缩略图轨道，按选择顺序并排展示并共用尺寸 token；轨道溢出时整体横向滚动，不新增独立文件覆盖层或第二条文件栏。文件缩略图使用 A 方案的“紧凑格式图标 + 两行文件名摘要”：外层保持图片缩略图的中性表面和尺寸，约 `44px × 50px` 的格式图标居中放在上方，文件名作为主要识别信息在下方最多显示两行，完整名称通过 `title` 和无障碍名称保留；当输入过多触发 `compact-grid` 缩略模式时隐藏文件名，只保留格式图标和完整名称提示，避免文字压住图标。同一格式家族共享色相，但颜色只用于小图标的顶部短色带、轮廓和签名，不再大面积填满缩略图，也不使用无语义的右上角识别点。每个受支持扩展名保留唯一签名，生成页已选文件和历史详情复用同一 SVG 图标规格，未知格式只显示中性 `FILE`，不得把用户文件名或原始扩展名插入 SVG。“清空”属于整个参考输入区的统一动作，必须同时清空图片和文件，不能只重置 `state.images`。选择历史任务只恢复任务表单和参考输入，不得根据任务原始 backend 自动切换顶部 API / Codex 授权来源或供应商；切换授权来源、供应商或 Images / Responses 模式也不删除文件、不弹移除警告。当前通道为 Images 时允许文件继续留在输入轨道，只有点击生成才通过失败预览明确提示切换到 Responses。文件任务被实际 API 通道领取后必须记录真实供应商名称与实际 backend，任务卡沿用普通任务的“供应商 · Responses”标签；历史 metadata 缺少供应商快照时不得猜测名称。生成页不提供最近参考文件列表，也不主动请求最近参考文件；历史任务 metadata 与历史详情中的文件展示继续保留。

输入区缩略图不使用常驻“编辑”文字按钮；可编辑图片通过点击缩略图主体进入编辑面板。上传入口和覆盖在缩略图上的加号都使用 SVG 绘制，避免文本符号在不同字体下偏离视觉中心。覆盖动作必须互不遮挡：右上角保留删除，左下角使用 SVG 加号 + `图库` 短胶囊表示保存到公用图库，视觉文案保持短、可点击区域稳定，并提供 `aria-label`。最近上传属于上传/参考图输入流程，应放在上传组件下方的全宽横向栏，而不是挤在公用图库短列里；横向溢出时隐藏滚动条，但必须支持触控板和普通鼠标滚轮横向浏览。最近上传缩略图的右上角动作必须反映真实引用状态：未引用素材使用危险色永久删除，被任务引用的素材使用中性隐藏动作并在确认层显示引用任务数，不能让用户误以为原图或历史任务会被删除。

最近上传栏标题旁提供整排图片显示的临时开关，只改变当前页面的可见性并保留栏位，不写入后端、不删除或逐张隐藏素材，刷新页面后恢复显示。

图像输入区右侧的公用图库快速选择和左侧上传组件应保持上半区等高，管理公用库按钮放在右侧底部并与左侧操作/最近上传栏对齐。公用图库列是辅助选择区，宽度保持紧凑，约为原 244px 宽列的三分之二，不抢占最近上传横向栏空间。不要让窄屏因为单列重排反而比宽屏显示更多最近上传缩略图；宽屏和窄屏都应遵守同一条“左侧最近上传横向栏”规则。

公用图库分类是可管理资源。分类名称、提示词用途、排序和图片归属应走后端持久化，不要用 `localStorage` 模拟服务端状态。图库抽屉内部优先采用上下结构：顶部用分类胶囊切换当前分类并预留折叠式“管理分类”区，下方保留全宽图片网格；不要常驻左右分栏去压缩图片区。排序不要暴露 `10/20/30` 这类稀疏序号，默认使用可见拖拽手柄直接重排分类和当前分类下的图片；图片卡片使用顶部全宽拖拽轨道作为排序入口，不把拖拽手柄叠在图片角落或混入操作按钮区。拖拽时必须克隆原分类行或原图片卡作为跟随鼠标的预览，移动过程中直接重排原列表占位，让分类行或图片网格提前腾出放手后的空间，不能只靠原位置半透明或插入线表达目标位置。分类切换和网格高度变化保持平滑过渡。

“管理公用库”和“管理模板库”属于同一类资源管理入口，应统一使用右侧 side sheet 家族而不是混用居中 modal。模板库使用窄抽屉，公用图库使用更宽的抽屉；两者共享同类 header、遮罩、关闭按钮和焦点返回规则。只有“添加到图库”这类一次性确认/录入动作保留 modal。

图库图片的“使用”动作应把图片和增强提示词上下文一起带入输入区。提示词增强内容应该作为参考说明或可编辑补充出现，不应无提示地覆盖用户主提示词。

### Prompt Editor

提示词编辑区是生成意图的主入口。主提示词、参考图说明、负面提示词、风格或尺寸参数要有清楚边界。“开始生成”和“管理模板库”属于右侧动作列，宽度应与图像输入区右侧公用图库列对齐，共用紧凑侧栏宽度，避免下方动作列比上方辅助区更窄。提示词编辑时 `Cmd+Enter` 触发“开始生成”，但不能绕过现有输入校验、队列提交和运行状态反馈。

自动增强提示词必须可见、可编辑、可撤销。来源于图库的提示词用途应作为参考层参与增强，而不是变成用户不可控的隐藏内容。

提示词查找/替换属于批量替换用的轻量次级工具，应从“清空”旁边的同一行展开，不使用 modal 或遮挡编辑区的浮层。打开时可临时隐藏最近模板 chip 来换取横向空间，但不能改变右侧“管理模板库”动作列宽度。查找使用精确文本匹配，不做正则，也不在输入时实时定位、选中文本或滚动编辑器；只有点击面板内“查找”才统计匹配数量。替换按钮直接执行全部替换，只作用于普通文本节点，必须跳过 `@` 图库、`#` 颜色和 `~` 片段这类原子 chip。替换后要同步可见编辑器、隐藏提交值、字数、图库输入和请求预览。短屏下按钮可以压缩内边距，但动作组不能溢出到右侧模板按钮下方。

提示词编辑器允许用富 DOM 承载 `@`、`#`、`~` / `～` 原子 chip，但 chip 内部不参与浏览器原生文本选中，整段选择时改用组件级选中反馈；三击提示词编辑器应选中包含 chip 在内的完整提示词。切换任务或重建编辑器内容时必须清除旧文本选区、悬浮收藏入口和片段浮层，不能让上一任务的临时操作状态泄漏到下一任务。外部网页粘贴进入编辑器时必须清理为纯文本，只保留必要换行。不要把网页 HTML 的 `style`、`class`、`data-*`、字体、颜色或块级标签结构插入可编辑 DOM。`@/#/~` 候选浮层和片段详情浮层属于临时操作层，应按视口定位并在短屏下内部滚动，不能被编辑器滚动层、模板 chip 行或面板边界裁剪。

提示词片段使用 `~` / `～` 及常见波浪号变体触发，生成短标签 chip。chip 可以隐藏长文本，但不能隐藏生成意图：点击 chip 必须能查看完整片段，并提供展开、编辑和移除路径。选中文本收藏片段使用选区末端浮动按钮，不在提示词面板新增常驻按钮，避免挤压主编辑区。

`~` / `～` 及常见波浪号变体只在文本开头、空白或常见标点后的独立 token 中触发片段候选，连续输入多个波浪号时按一个触发符处理；不能把 `1~2`、`A~B` 或中文语气词误判为片段引用。提示词片段应走本地后端持久化，不使用 `localStorage` 作为长期数据源。

提示词模板用于较长的可复用结构，不是图库、不是片段 chip，也不是外部模型模板市场。主面板把字数统计放在提示词标题行的输入框列右侧，并与“提示词”标题文字底部对齐；“管理模板库”入口放在“开始生成”按钮下方的生成按钮列右侧，宽度填满生成按钮列并与“开始生成”一致，使用与“管理公用库”一致的尺寸和填充按钮样式，并保留模板图标；最近模板短 chip 放在输入框下方的输入框列右侧；提示词“清空”按钮保持在输入框下方左侧的原位置。清空按钮、最近模板 chip 和管理模板库按钮属于同一行次级操作，垂直居中对齐，不做顶对齐。两者都不是覆盖在按钮或输入框上的浮层，也不占用编辑器主体空间。最近模板只保留最多一行短 chip。完整浏览放在右侧抽屉。抽屉顶部工具条使用搜索框加“新建 / 导入 / 导出”三枚等宽按钮，所有控件同高、同一基线对齐；导出是本地资料迁移动作，必须有明确点击反馈。抽屉使用收藏/最近筛选、可增删改的分类胶囊和两列瀑布流卡片，分类胶囊自动换行，不出现横向滚动条；卡片封面按图片真实比例展示，不裁剪成固定比例，同时保留短标题、标题、内容摘要和轻量元信息，让用户能用画面比例和文本同时识别模板。模板可从历史任务结果读取缩略图，但缩略图只能辅助识别，不能替代短标题和内容摘要。导入/导出模板包是本地资料迁移能力；导入常见社区 JSON/Markdown prompt pack 时，只解析提示词、标题、标签、分类和缩略图等通用字段，不把外部模型文案变成默认 UI 或默认模板。模板插入、替换、复制、编辑和删除都必须作用于可见提示词内容或本地模板 JSON，不做隐藏提示词注入；默认模型语境保持 `gpt-image-2`。

### Segmented Controls

按钮式选项组使用 `.radio-group` 和 `.radio-btn`，适合提示词模式、尺寸、比例、方向、质量等少量互斥选项。控件外层使用普通 `.field` 容器，标题用 `span` 或 `.field-label`。

一维 `.radio-group` 和顶部连接来源切换使用 `segmented-indicator` 共享底板；多行或跨行布局如比例组不启用滑动底板，避免滑块在二维网格里产生跳跃感，但比例按钮自身仍需要用背景、文字和轻微缩放过渡反馈选中变化。

滑动分段控件的外层 padding、按钮高度和滑动底板圆角必须由同一组变量控制，确保选中底板与外框的上下留白一致；二维比例组可以覆盖 padding 和高度，但不要启用滑动底板。

不要把按钮组包进 `<label>`。浏览器会把点击 label 文本或空白区域的行为转发给内部第一个按钮，造成当前选项跳回第一项。只有真实输入框、文本域、选择框、滑杆等表单控件才使用 label 包裹。

### Cards And Lists

任务、图库图片、上传图和历史记录可使用卡片或列表行。重复项卡片使用 `surfaceSoft`、`line`、`large` 圆角。卡片内按钮组要保持固定排列，不因文本长度改变卡片高度。

列表适合高密度状态，卡片适合需要图片预览的资源。不要把每个设置项都做成卡片。

左侧任务列表和队列状态是扫描型界面。任务标题应优先使用短名称、模式、尺寸、数量和状态摘要；长 prompt 只能作为次级预览，不能成为列表主标题。任务卡默认不使用常驻状态灯，未读点只表示未读更新；完成、失败、运行中等状态必须用短状态文本和卡片底色表达。任务项采用紧凑的柔和扁平薄卡：四边一致的细边缘和从顶部到底部约 `3%` 级的平滑明度渐变共同表达表面，不使用独立顶部高光线、底部厚度暗边或接触阴影。default、hover 与 selected 静态时都不抬升；hover 只轻微调整底色与边缘，selected 使用整卡低饱和主题实色填充、普通 `1px` 边缘、常驻勾选框和更清晰的文字层级形成复合反馈，不依赖粗描边。按住前方卡面时下沉约 `2px`，开始横向滑动后立即取消垂直位移，松开后按既有节奏回位；`focus-visible` 使用独立的外部焦点环。禁止左侧强调条、单边粗线、侧边选择轨和双层轮廓。深色主题的 selected 卡面必须相对 default 建立可感知的整体明度层级，同时保持纵向渐变克制，并轻微提高标题与元信息清晰度。状态变化不得改变卡片宽高、内距或内容行数，`prefers-reduced-motion: reduce` 下取消非必要位移。图片槽位用固定小方块表达数量和状态，终态任务不再重复显示“1 张 / 成功 / 失败”这类文字摘要，只有运行、等待或重试时才追加必要的短文本。任务卡固定采用“状态 + 模型家族 mini icon + 活动图片数 / 时间”“长标题/提示词”“画幅 · 分辨率 · 供应商 / 完成时间或失败动作”三行结构。状态词只出现一次，运行中图片数显示为“`N 张`”而不是再次写“生成中 N”；第一行右侧复用同一个时间槽，运行时显示“计时”，终态显示“耗时”。mini icon 与左上模型控制共用 OpenAI、Google Gemini 的本地品牌标识资源；13px 下保持完整轮廓和等比缩放，禁止裁切、拉伸或另绘近似图形，不显示 Pro / 2 / Lite 后缀，完整具体型号通过悬停、无障碍名称和任务详情提供。画幅、分辨率优先读取冻结快照，再回退待提交 canonical 请求和旧参数；缺画幅时从真实尺寸约分，GPT 规范分辨率统一显示 `1K / 2K / 4K`，自定义尺寸或旧任务无法可靠分级时显示真实像素，不伪造默认值。让短且固定宽度的信息先建立扫描锚点，长标题独占一行，不和时间、状态、失败动作争宽。失败和重试信息在列表中只显示短动作状态，例如“可手动重试”“不可重试”“重试中”，并放在第三行右侧固定槽位；完整失败原因留在 tooltip、详情区或预览区，不能用长错误文案撑高任务卡。任务与任务之间采用紧凑列表行节奏，默认间距保持 4px 级别；失败任务用浅红卡面保留异常语义，同时沿用与普通任务相同的薄卡边缘和空间层级。完成态继续由状态文字和状态点表达，不能复用 selected 的整卡层级。

所有任务状态统一使用 `66px` 卡片高度；运行、提交、排队和终态只能改变状态内容与语义色，不能通过额外高度制造层级。

左侧任务筛选是持续浏览状态，不是临时菜单。筛选面板打开后应作为搜索框下方的内联区域参与布局，把任务列表自然推下去；不要用绝对定位 popover 覆盖第一条任务，也不要在点击任务列表时自动收起导致列表跳动。筛选开关、清空和 `Esc` 可以显式关闭或重置筛选面板。任务搜索输入必须避开浏览器自动完成，并以应用内搜索状态为唯一真源；页面加载、恢复或浏览器自动填充阶段注入的 API Base URL 等其他字段历史值不得成为搜索条件，只有真实键盘、粘贴、拖放或指针交互才能解锁并提交查询。

侧栏宽度拖拽必须实时跟手。拖拽起点使用已知宽度状态，不在 `pointerdown` 同步读取整条侧栏布局；拖拽过程中的宽度变量只写在侧栏元素本身，不能写到根节点造成整页样式失效；拖拽期间用预置透明遮罩承担 cursor、user-select 和命中测试，不给 `body` 添加全局拖拽 class；拖拽过程中只更新宽度，不触发任务列表重绘、预览高度重算或持久化写入，松手后只持久化最终宽度；`workspace` 容器查询由 CSS 自动重排单双栏和预览网格，禁止 JavaScript 测量或同步预览高度。

独立历史库使用直接三栏布局，不显示左右分栏手柄，也不为拖拽功能保留网格占位轨道；拖拽能力由覆盖在两条真实栏边界上的透明 `12px` 命中区承担，常态不绘制背景、握柄、阴影或分隔线，只用 `col-resize` 光标反馈可拖拽。中间结果区外层水平内距保持左右 `12px` 对称，任务列表只向右延伸 `12px` 让滚动条抵达面板边缘；内容左内距使用 `4px`，右内距使用 `15px` 补偿约 `11px` 的稳定滚动条槽，使卡片到左侧栏滚动条、到右侧任务滚动条的可见空白都约为 `15–16px`。结果区外层底部不保留固定内距，安全区留白放进可滚动任务列表，避免视口底部形成独立色块。历史任务卡不显示缩略图复选框，普通点击单选，`Cmd/Ctrl` 点击切换非连续选择，`Shift` 点击扩展连续范围；管理态提供显式“选择任务”触控兜底，真实空白点击和 `Escape` 清空选择。单选与多选都使用无描边、无阴影的整卡主色实心填充和对应前景色，焦点另用外部焦点环，不能再增加勾选角标或选择轨。拖拽过程中只记录最后一个指针坐标，栏宽通过单个 `requestAnimationFrame` 逐帧跟手；拖拽开始时缓存三栏可用宽度，移动过程中不再同步读取整页尺寸。拖动期间缩略图卡片保持既有宽高，只由 flex 行随可用空间自然换行，不能周期性批量重写宽高或切换内容可见性造成图片闪烁；松手后只执行一次 justified 重排、持久化并校正当前任务位置。拖动中不重建任务 DOM、不写入存储。左右侧栏与中间结果区直接相邻，不能额外插入空白分隔列或贯穿分栏的装饰线。

历史库返回体验以“回到离开时正在看的内容”为目标，而不是默认跳回最新任务。每个浏览器标签页独立记住当前查询、首个可见任务和相对顶部偏移；从无显式参数的历史入口返回时只恢复覆盖该任务的一个约 50 条窗口，再精确校正偏移。搜索、任务详情、排序、视图和筛选等显式 URL 仍是用户更强的意图；锚点已删除时安静回退当前排序的普通首窗。位置保存不能扩大窗口化边界，页面继续只挂载最多 300 张任务卡。

历史库筛选栏先放收藏和标签，再按“类型、月份、提示词模式、质量、比例、方向、后端、供应商、归档”排列；类型使用任务索引中的真实 `generate / edit` 字段映射为“文生图 / 图生图”，不能用前端缩略图或输入数量猜测。标签管理保留在侧栏文档流中，不打开额外弹窗；创建、改名和删除影响数量必须清晰可见。每个分类标题都是可折叠的原生交互，默认展开常用分类，默认收起一般用户较少使用的“后端 / 供应商”；如果 URL 已带后端或供应商筛选，则自动展开对应分类以暴露当前状态。筛选项的名称保持主信息权重，数量以同一按钮内的细分隔线、较小字号和弱色独立成辅助列，不增加第二层胶囊或描边，尤其不能让 `9:16` 之类的比例值与计数连读成一个数字。

历史结果区的工具栏下方使用一条紧凑活动筛选栏，确保从 URL 或标签页位置恢复的旧条件不会隐形生效。搜索、常规筛选、收藏、具体标签和无标签分别形成可单独移除的 Chip；“清除全部”只清空搜索与筛选，继续保留当前排序和网格/列表偏好。没有条件时整栏完全隐藏。单行状态下筛选摘要、Chip 列表和右侧清除动作共用垂直中线；Chip 按可用宽度自动换行，筛选摘要和右侧清除动作固定可见。窄视口把 Chip 放到摘要与清除动作下方并继续换行，同时在“任务筛选”按钮内显示活动条件数量，不能裁切条件或让长文本制造页面横向溢出。单个异常长 Chip 可省略可见文本，但完整内容继续保留在标题和可访问名称中。

历史库结果区顶部只承载稳定的浏览状态：视图和排序保持连续分段控件，刷新保持同高的实心 SVG 图标按钮并提供完整 `aria-label` / `title`；选择任务前后都不能把这组控件替换成批量工具栏。任务备份与导入备份移到无任务详情时的右栏管理态，避免与视图、排序争抢同一水平空间；窄视口在刷新旁显示管理图标，按需打开同一右栏抽屉。顶部控件统一使用无描边、无阴影的实心色阶，浏览分段以 `surfaceSoft` 承载并用 `primary` 表达当前项，不能产生横向滚动。

收藏是独立于标签的星标状态，五角星只表达任务收藏，不与输出精选或任务选择复用；卡片星标位于图片操作层外，已收藏时常驻，未收藏时只在普通浏览的悬停或键盘聚焦中显露。进入批量选择或显式选择模式后，未收藏星标隐藏并交由右栏批量整理，已收藏星标继续保留，避免一组空星标制造噪音又不丢失真实收藏信息。卡片正文最多显示两个标签，其余合并为 `+N`，详情区展示全部标签并提供编辑入口。多标签筛选采用“同时拥有”语义，“无标签”和具体标签互斥。历史库采用统一选择集合：零项显示备份和导入管理态，一项显示任务详情，两项及以上显示批量操作；管理态的“选择任务”让触屏用户进入点按切换模式，桌面继续使用 `Cmd/Ctrl` 与 `Shift` 系统快捷键。批量操作统一放进右侧详情栏：标题显示已选数量并提供退出选择；“整理”与“导出”是互斥展开的内联分组，整理承载收藏、取消收藏、添加标签、移除标签、归档和恢复，导出只承载两种轻量导出；任务备份保持一级动作，删除独立置于底部危险区并保留就地二次确认。动作行使用无描边实心色阶和统一 `16px` 描边 SVG，不嵌套卡片。退出选择或关闭单任务详情必须清空选择、当前详情和 URL 的 `task` 参数，立即回到管理态，不能保留失效的“已选择”文案；在任务网格或列表的真实空白处单击、或按 `Escape`，也执行相同取消动作。卡片、收藏按钮、工具栏和滚动条不能被空白取消逻辑误伤。小视口首次多选不自动弹出抽屉，而是在任务区底部显示“已选数量 / 批量管理”入口，批量操作抽屉的关闭只隐藏面板且不能清除选择。

单任务详情的“导出”继续使用靠近触发按钮的主题化选择层；批量导出直接在右栏内联展开。轻量导出都只保留“仅图片”和“图片＋提示词”两种选择，任务备份不能混入导出选择层。单任务选择层必须支持 `Escape`、点击外部关闭、焦点返回和明确的准备中状态；批量内联状态也要就地显示准备、成功或失败且保留当前选择。多任务始终汇入一个 ZIP 并按任务分目录，不能在浏览器中先读取大文件或连续触发多个下载。已选任务的“任务备份”是右栏一级动作，打开备份面板后默认锁定“已选任务”范围。

任务备份与上述两种轻量导出保持清晰分层：用户可从已选任务、当前筛选结果或全部历史任务开始，筛选/全部范围由服务端完整处理，不受历史页最多 300 张已挂载卡片影响。历史任务引用的输入图、遮罩或参考文件已经缺失时，不阻断仍完整的提示词和输出结果导出；进度摘要持续显示受影响任务数和缺失文件数，并明确这些输入不会包含在备份中。导入按“选择 ZIP → 上传与校验 → 分类预览 → 明确确认 → 恢复结果”推进，已有同 ID 任务只显示重复或冲突，绝不提供覆盖选项；文件选择继续使用真实原生文件输入语义，但按钮、文件名、边框、焦点和禁用态必须继承深浅主题 token，不能露出浏览器默认灰色控件。长任务用主题主色与浅色 token 组成的可恢复进度承载，完成包只提供一次性下载。下载触发后用独立成功摘要替换已失效的范围表单，只保留一个主级“关闭面板”动作；下载前的“不下载并关闭”保持次级并明确会放弃结果。弹窗延续安静工作台层级，在深浅主题、窄视口、键盘和长文本下都让范围、状态与主要动作保持同一阅读轴。

队列管理集成在左侧任务列表顶部的“活动任务”状态区，不再单独打开队列抽屉。“活动任务”内部先显示运行中任务，再显示等待中任务；运行中按本次进入运行状态的时间正序稳定排列，新任务追加到底部，不按可复用的执行通道槽位排序，等待中继续按可拖拽的队列位置排列。它不参与历史时间分组，不占用历史展开位。“活动任务”本身必须可折叠，任务很多时收起后保留标题和数量摘要，让今天、昨天和最近 7 天仍可展开和切换；折叠状态使用独立轻量状态，不复用历史时间分组的展开 key。运行中任务较少且展开时，最后一个运行中任务下方必须紧接默认展开的历史时间标签和内容，避免中间出现大面积空白断层；任务多到独立滚动时，标题保持粘性，并在滚动区底部保留一条弱分隔，使活动任务与历史任务始终能被辨认。实时状态、生成结果、缩略图或计数变化需要重绘活动区时，按首个可见任务 ID 与相对偏移恢复独立滚动锚点，不能把用户拉回顶部。等待任务不少于两个时以整张卡片作为排序入口：桌面竖拖直接排序，触屏和手写笔长按后竖向拖动；跟手卡片与等高占位表达当前落点，靠近滚动区边缘自动滚动。短按继续查看任务，普通触屏竖滑继续滚动，横向手势继续露出置顶 / 取消机械抽屉；不再叠加拖拽手柄或上移 / 下移悬浮条。键盘 `Alt+↑ / Alt+↓` 和右键菜单保留精确排序能力，首次提示在用户完成第一次拖动后收起，减少动态效果时取消抬升和补位动画。运行中任务向左滑动露出“停止”，向右滑动只提供短距离阻尼并回弹，不绑定复制或其他隐含动作；停止继续经过现有计费风险确认。等待任务向右滑动露出“置顶”，向左滑动露出“取消”；取消保留带取消标记的历史记录，不能退回会同时删除历史的旧删除路径。运行中与等待中合计不少于 2 个时，“批量管理”左侧显示“批量取消”；点击进入现有批量选择模式并预选全部活动任务，用户可取消勾选，确认后运行任务停止、等待任务离开队列，二者都保留带取消标记的历史记录。批量工具栏使用紧凑的圆角方形多选控件，视觉勾选框不挤占任务缩略图但保留不小于 24px 的实际命中范围；“全选等待中”只选择当前真实等待队列并在队列为空时禁用；“归档”独占一行，“删除 / 取消任务”并列，退出操作统一复用顶部“批量管理”开关。顶部队列状态 chip 只做状态汇总和跳转入口，不承载具体队列操作。

活动任务分区标题与总标题沿用同一紧凑格式，分别显示为“运行中 · 数量”和“等待中 · 数量”，名称和数量不能被推到容器两端。带完整边框的“活动任务”和历史时间一级标题相对整条标题栏居中，右侧箭头以等宽对称轨道占位，不参与标题中轴；无边框的“运行中 / 等待中”二级分区标题继续左对齐，与下方任务卡建立稳定阅读轴。

生成页左侧任务历史采用轻量工作区模型：只显示活动任务、今天、昨天和最近 7 天，并在最近 7 天下方提供常驻“历史库”入口；入口不受近期任务数量、搜索或筛选结果影响，无近期任务或仅有活动任务时仍可访问完整历史。不在生成页继续渲染无限增长的“更早”大组。三个时间组分别按终态活动时间从索引读取有界摘要和准确总数，不能让今天的大量任务挤掉昨天或最近 7 天；首屏每组最多加载 50 条，展开组末尾按 50 条继续分页，50 条只是首屏负载预算而不是历史访问上限。靠近组尾时自动续载必须保留已挂载任务卡，只在尾部追加新页；正常加载过程不插入可见文案、不改变列表高度或滚动位置，失败时才在尾部显示重试。批量模式可以按当前时间组和筛选条件从服务端全选尚未加载的匹配任务，破坏性删除仍使用明确计数和二次确认。终态活动时间使用首次进入当前终态时固定的 `terminal_at`，旧记录依次回退 `completed_at`、`created_at`；`updated_at` 只表示后续维护时间，清理失败项、精选或查看不能改变历史排序。从历史库选择“在生成页查看”时，如果目标仍属于这三个日期组但藏在尚未加载的分页中，生成页按相同索引顺序补载到目标并展开所属组；分块渲染和高度布局完成后再即时把已选卡片滚动聚焦到可见区域，不用展开动画或长距离平滑滚动赌定位结果。如果目标更早或已归档，则只在本次选中期间增加单条“当前查看”临时组，离开该任务即恢复轻量分组，不把临时入口扩展成长期“更早”列表。生成页时间分组采用单展开组模型，而不是多组同时展开。默认展开第一个非空时间组，优先级为今天、昨天、最近 7 天。当前组只在中间内容区出现一次；当前组之前的非空组固定在顶部，当前组之后的非空组固定在底部，避免重复时间标签。顶部和底部时间锚点的文字点击负责切组并回到组首，箭头负责展开并切换到相邻时间组；不保留“全部展开 / 全部收起”。历史区出现真实滚动且用户离开最新任务位置时，在当前时间标签下方中央悬浮轻量向上箭头，不推动任务卡；任务在用户停留旧位置期间进入终态时以角标累计新任务数。用户原本就在最新组顶部时，新任务插入后继续固定在顶部，不让旧卡滚动锚点把最新结果推走。点击只回到第一个非空时间组的组首，不选择任务或改变右侧详情；到达最新位置后隐藏并清零，搜索和批量模式中不显示。切组、箭头入场和滚动都同时支持 `prefers-reduced-motion`，新任务提示复用实时任务事件而不增加轮询。完整历史浏览、搜索、归档筛选和大批量任务管理归属独立 `/history` 页面。

已完成、失败和部分失败任务卡片在第一行右侧显示耗时，第三行右侧显示本地完成时间，用短格式辅助查找，完整日期和秒级时间放在 tooltip。运行计时和终态耗时必须复用第一行同一槽位，状态变化时不能上下跳位。不要为时间戳新增一整行或增高卡片。所有可操作任务卡共用藏在实体卡面后的机械抽屉：普通历史任务向右露出“归档”、向左露出“删除”；运行任务只允许向左露出“停止”，向右只阻尼回弹；等待任务向右露出“置顶”、向左露出“取消”。松手只把卡面稳定在固定开启位置，用户再次点击露出的实体按钮才执行。任务项外层只是透明的定位与手势结构，完整卡面承担细边缘和轻微垂直明度渐变；卡面移开后直接露出侧栏大背景，只留下使用主题化实色底与同类轻渐变的操作按钮。卡面和按钮都不使用独立高光线、底部厚度或投影，也禁止单卡托盘底板、导轨、槽体边框、槽体内阴影、进度渐变、外发光或覆盖卡面的色幕；按住操作按钮时下沉约 `2px` 并轻微变暗。闭合态卡面必须与任务行同宽，完整显示统一四边和原有固定宽高；滑动时缩略图、文字、边缘和圆角作为一个刚体共同位移，不能只移动内容层、某一条边或动态改变左右 inset。后置按钮固定在任务行两侧，不随卡面探头、反向移动或在开启时跳位。卡面可越过任务列表的内容内边距，最终只在侧栏最外缘被遮挡；左右都不能被中间滚动层提前切断而留下侧栏底色空带，也不能因此产生横向滚动。外层任务卡宽高、选中层级和列表间距保持不变，方向锁优先保留垂直滚动，未到露出阈值或没有映射动作时平稳回弹。单卡历史删除按钮和等待任务的取消按钮本身承担明确确认，点击后直接执行，不再打开旧确认层；运行停止和批量危险动作继续保留确认。归档使用短促卡匣收纳，删除使用短促落闸，两者完成后立即移除并用 FLIP 让下方卡片补位。任一时刻只打开一个抽屉，外部点击、滚动和 `Esc` 收起；减少动态效果时取消卡面过渡、退出动画和补位动画，为保持操作可达，抽屉露出位移即时完成。仅批量模式禁用单卡手势。右键菜单继续承载查看、复制、打开输出目录和当前任务状态对应的抽屉动作，并支持菜单键 / `Shift+F10`；焦点卡片的 `Delete` 露出左滑侧动作，`Shift+ArrowRight` / `Shift+ArrowLeft` 分别露出当前卡片右滑 / 左滑动作，没有映射的方向不执行。右键菜单使用 popover 层级、短标签和固定最小点击高度，不能触发整条历史列表重绘。

运行中任务的“停止”抽屉使用高于普通左滑动作的明确位移阈值，轻微斜拖必须回弹；从按下起捕获指针，松手时必须使用最终指针坐标完成方向与阈值判断，不能因快速拖动、大跨度移动或浏览器合并中间事件而错误回弹。指针捕获丢失、窗口失焦或页面隐藏时立即复位，不能把卡面留在任意中间位置。

任务历史组的 Grid 轨道必须允许内容收缩到可用列宽；任意长度的提示词、标题和元信息都只能在卡内省略，不能通过内在尺寸撑宽闭合卡面或扩大横向滚动范围。

图生图或带参考图的任务只有在生成结果与第一张参考图同时存在且 URL 不同时，才把生成结果作为完整主缩略图，并在左上角显示固定尺寸的第一张参考图角标；主图保持原比例 `contain`，使用弱主题实色底承接横竖图留白，不能裁成正方形。参考图角标使用实色底面、无描边、无阴影，以固定占位表达来源，不能继续把两个透明 `contain` 图层斜向叠放，否则横生竖与竖生横都会形成十字拼贴，图片元素外框阴影还会制造残影。运行中、排队中或提交中的图生图任务在尚无生成结果时只显示一张参考图和居中小型转圈，禁止复制同一参考图伪造双层；已有首张结果后才切换为结果主图 + 参考图角标并保留转圈。纯文生图保持无角标的单图缩略图，图生图继续通过参考图角标或无障碍名称表达类型。历史任务卡片应优先加载生成结果和上传参考图的本地缩略图缓存，不能用原图级输出图或参考图冒充缩略图；缺失缓存时可以按需生成后再显示。历史/详情保留最长边 768px JPEG 缓存，左侧 48px 任务卡单独使用最长边 256px WebP，避免为小卡解码大图；两类缓存都属于可重建数据并随任务或对应输出删除。悬停或键盘聚焦时只允许轻微强调参考图角标，不能淡出主结果或依赖 hover 作为唯一识别线索。

历史网格中的多图输出任务使用实体相册卡片隐喻：主缩略图后按其余输出数量绘制最多 3 张实心卡层，分别以 `6px / 12px / 18px` 等距向右下错位；单图与多图封面统一使用弱中性内描边，不因多图额外增加绿色边框。底层采用 D2 石墨灰三级递进：浅色为 `#e3e8e5 / #cbd3cf / #b2bdb7`，深色为 `#4a5550 / #3a4641 / #2d3833`；内部层只用极弱同色分隔，当前最外层才保留弱轮廓和短阴影。必须让用户不依赖数字也能数出 2、3、4 层，不能只画透明圆角线框或每层绿色描边。卡片右上角不显示图片数量或“查看中”角标，当前任务只通过整卡边框与背景表达；深色主题与浅色主题都必须保持实体卡层可辨认，卡层不能覆盖主图或复选框。

历史详情中的结果图操作层属于图片本身：精选、单图下载和加入参考图按钮只在图片媒体区域悬浮或聚焦时出现，渐变遮罩也只能覆盖图片底部，不能延伸到下方优化提示词。顶部打包下载沿用按钮式链接外观，不显示文字下划线。

历史详情空状态只保留一级“任务详情”标题，不再用“任务详情”眉题叠加“历史任务”标题。顶部操作栏按结果与管理两组组织：结果组依次为生成页查看、单图下载或打包下载，管理组依次为导出、归档和危险操作。无精选时危险操作为“删除”且作用于整个任务；存在精选时原位切换为“删除未精选”，全部输出均已精选时保持禁用，必须取消精选后才能删除整个任务。多选精选时下载动作原位切换为“下载精选”。精选状态不能新增上下文工具栏或布局行把图片区下压，精选变化要清除待确认删除状态。详情栏较宽时两组按同一行显示；容器宽度不足时结果组使用两等分列、管理组使用三等分列，形成稳定的 `2 + 3` 两行布局。视觉顺序、DOM 顺序与键盘顺序必须一致，动作行按真实高度参与详情栏滚动布局，不能形成右下孤立按钮，也不能被下方图片区覆盖或裁切。

历史详情任务标题始终单行省略，避免长标题形成正文段落感，完整内容通过原生标题提示保留。逐图优化提示词、原始提示词与优化提示词共享 `13px / 1.6` 的排版尺度和相同字体族；提示词标题与正文使用相同字号，仅以 `700 / 400` 字重建立层级，复制按钮不参与这套正文排版。

右侧预览区在任务切换时要保持空间连续。已有输出卡片按结果槽位复用：同张数任务切换时当前图片保持完整可见，新图片完成 load/decode 后再替换当前图片；输出张数变化时先预加载并解码目标输出图，再在同一帧提交新布局和新 `src`，避免先绘制空卡片、broken image 占位或上一任务图片被新布局拉伸。只有预加载失败时才隐藏复用槽位并清掉旧 `src` 作为兜底。不要用固定短超时强行换 `src`，也不要在加载下一张时降低旧图透明度，避免整块预览先清空或轻微闪白。切换时保留上一帧方向布局，等新图加载后再同步方向类，并支持 `prefers-reduced-motion`。两张方图和两张横图使用单列两行预览，让单张结果尽量放大；两张竖图保持并排方便对比。四张方图和四张竖图都应固定为 `2x2` 预览，不回落到自适应三列。结果卡操作里，“加入参考图”是直接进入图像输入的主动作，“暂存”是放入待加入参考图栏的次级动作；按钮使用内容自适应宽度，保持紧凑点击目标，不要铺满整张图，也不要用两个同权重长按钮表达相近含义。

待加入参考图栏必须作为上传区内部的单行横条参与布局，与原上传内容共享既有宽高；出现时通过压缩缩略图和上传入口腾出空间，不能覆盖内容、抬高上传区或引入横向滚动。

待加入参考图栏同时提供追加和替换两种明确动作。“全部加入参考图”追加到当前图片输入；“加入并替换参考图”只替换当前图片输入，保留 Responses 参考文件。替换必须先完成所有暂存图读取，再清理旧图片，读取失败时不能丢失当前输入。

结果卡的“提示词”浮层用于核对生成意图。默认展示“原始提示词”和“优化后提示词”的并排对比，长文本在各自区域内滚动；如果 `prompt_for_model` 因图库、片段或参考说明展开而不同于原始输入，只在折叠详情里展示“实际提交提示词”，避免干扰主对比。

多图结果的整理入口必须克制。精选入口只在多图或多槽位结果中显示；单图结果不显示精选按钮。未精选卡片右上角只保留低调的圆圈勾选图标按钮，hover/focus 时显示“精选”短文案；已精选卡片必须用主色填充胶囊、同一圆圈勾选图标、`已精选` 文案和主色描边共同确认状态，不能只靠图标变化区分。五角星只用于任务收藏。预览标题区只在已有精选时显示“已选 N/M”“只下载精选”和“删除未精选”。精选、取消精选和删除未精选都是用户正在查看时的主动整理，不能让任务重新变成未读；删除未精选属于破坏性本地文件操作，必须二次确认，并在确认后同步刷新 `outputs`、`output_urls` 和用户可见计数。不要为多图整理新增大面板或常驻工具栏。

图像输入缩略图在空间不足时可以切换到紧凑尺寸，但仍应优先保持单行横向流动并允许横向滚动；不要在第三张图就切成两行，让右侧空白无法使用。横向溢出必须有可见滚动条，并支持普通鼠标滚轮在该区域内横向移动。

### Modals, Popovers, Drawers

弹窗用于图库、确认、长表单和批量管理。弹窗内可以滚动，但标题和主要关闭动作要稳定。弹窗高度变化需要平滑过渡，参考当前模式切换的高度槽动画。

Popovers 用于局部设置，如压缩、筛选、更多操作。Drawers 用于任务详情或全局设置，不用于短确认。

### Queue And Status

队列状态必须清楚区分等待、运行、成功、失败、取消。状态文本要和颜色一起出现。失败状态要显示可操作下一步，如重试或查看错误。

队列面板的运行中和等待中分区应紧密相邻。运行中分区按内容高度自然增长，只设置最大高度和内部滚动；等待中分区接住剩余空间。不要用 `space-between`、固定百分比高度或空白占位把等待中任务推到面板底部。

任务数量和状态摘要必须对齐用户能看到的结果槽位。继续生成、重试失败槽位、接受成功结果等流程都不能把旧字段里的连续数组位置误当成真实槽位。成功数只代表有可显示结果的 completed 槽位；运行、失败和等待状态要保留原始槽位身份。

重试文案必须只描述真实状态。首轮多图生成中，如果某个槽位先失败但任务仍在继续生成，不能显示“重试中”；只有自动重试轮次或用户手动重试失败槽位时，才显示“等待重试”或“重试中”。`IncompleteRead(...)`、timeout、network 这类错误可归类为上游响应中断，但界面应同时保留可操作下一步，例如继续等待、重试失败图片或接受已成功结果。

页面标题、顶部状态和侧边栏摘要可以显示当前任务状态、队列数量和未读数量，但必须保持短句，不能替代任务详情。

### Auth Source And API Settings

授权来源设计只表达当前请求会走哪条通道，不承载账号状态、账号用量或轮询管理。`Codex` 表示复用本机 `~/.codex/auth.json`，`API` 表示使用 WebUI 保存的官方 API 或兼容中转站供应商配置。当前供应商旁的齿轮始终打开 `API 设置` Tab；`网络` 位于第二个 Tab，语言下拉选择位于第三个 `语言 / Language` Tab，切换后即时生效；存储和通知位于第四个 Tab，只有存储路径保存需要提示重启 WebUI。网络出口与授权来源、供应商、模型和协议选择互相独立：默认 `系统` 继承既有环境代理，`直连` 显式绕开环境代理，`自定义` 仅接受不含凭据的 HTTP(S) 代理 origin；三者复用一维分段控件的共享滑动底板，不提供自动选路或 SOCKS。保存无需重启，只影响后续开始的执行尝试，运行中的尝试保持其开始时冻结的出口。连接检测只由用户点击触发，并测试生成页当前实际选择的供应商 origin；Codex 绑定对应 ChatGPT origin，外部供应商 origin 只能由后端从已保存配置解析，不能接受浏览器传入的任意 URL，也不发送 API Key。没有生成页选择上下文时才回退到当前授权来源与活动 API 供应商，应用启动时不得自动请求网络。`Codex Image` 与 `Codex Responses` 是生成页供应商菜单中的两个稳定内置绑定：前者直接传递尺寸、质量和编辑参数，后者走兼容通道并支持联网搜索工具流；两者都使用专用标识，且选择必须以 binding 形式进入任务快照。Codex 仍是本机 OAuth 工作流，不允许被外部供应商设置、认证字段或模型绑定配置覆盖。

网络 Tab 在代理字段下方直接提供一组全局请求策略，不增加嵌套卡片：桌面端“单次请求超时”和“失败后重试”并排，窄屏改为单列。超时使用分钟输入，范围 `1–30`、默认 `10`；重试表示首次失败后的额外次数，范围 `0–5`、默认 `2`，只用于既有规则判定为可重试的瞬时失败。两项同时覆盖所有供应商的生成与编辑，并在任务开始执行时与网络出口一起冻结；运行中修改不改变该任务，每次内部重试都获得新的完整超时窗口，队列级故障转移规则保持不变。连接检测始终使用独立的短超时且不读取重试次数，避免用户把检测误变成长时间生成。若兼容环境变量提供了超出界面范围或非整数的正超时，运行时继续尊重该值，界面显示可编辑默认值并明确提示环境覆盖来源，直到用户显式保存。

`API` 分段按钮被选中后再次点击仍只保持选择状态，不打开设置、不产生隐藏手势。系统设置必须通过右上供应商齿轮或输出设置里的 `系统设置`显式打开，并始终进入 `API 设置`；`网络` 是第二个 Tab，`语言 / Language` 是第三个 Tab，`存储与通知` 是第四个 Tab，不再保留左下角独立存储设置入口，让入口可见、可解释、可测试。语言选择使用下拉菜单，不使用多按钮切换；语言 Tab 标题要以当前语言名称配合英文 `Language` 作为定位锚点，避免误切语言后找不到入口。语言字段按“标签—控件—说明”保持清楚的 `8px` 级纵向节奏，短高桌面的生成参数紧凑变量只能作用于生成设置网格，不能把系统设置字段压成零间距。网络“当前出口”是只读中性摘要，使用完整实心浅色表面，不使用 side-tab 式单边强调线。语言、生成页供应商、API 绑定的模型和协议，以及主模型候选都使用同一套主题化浮层，浅色/深色状态继承当前面板、边框、文字和选中态，保留键盘导航与可见焦点，不能回退成浏览器白底原生弹层。若触发器处于可滚动或裁剪容器，菜单必须提升为页面级固定浮层、随滚动和缩放重定位并留在视口内，不能被设置面板裁剪；菜单滚动轨道和滑块使用同一主题令牌。API 设置里供应商选择和供应商编辑必须分层：默认顶部显示所有已保存供应商的快捷选择和总数，供应商卡片使用统一双列网格，自定义供应商可选一枚彩色 emoji 作为安全的文本身份标识，点击供应商只切换当前使用项；供应商不超过 10 个时完整展开且不出现内部滚动条，超过 10 个时显示名称搜索并把同一网格限制为约 4–5 行高度，避免为数量阈值切换完全不同的组件或制造列表与设置面板双滚动。每次打开设置、取消编辑返回列表或切换供应商后，只在供应商列表内部把当前卡片定位到可视区域中部或最近位置，不得带动整个设置弹窗滚动。`新建供应商` 是同一网格里的弱底虚线添加卡，视觉上与供应商选择对齐，但不作为已保存供应商选项。进入编辑或新建状态后，完整供应商选择区必须隐藏并设为不可交互，只保留紧凑的编辑状态栏和表单，保存或取消后再恢复列表，不能继续展示一组看似可切换但实际被拦截的卡片。供应商数量大于 1 时，排序入口放在“API 供应商 / N 个供应商”标题行右侧；排序是集合动作，不放进单个供应商详情卡。进入排序后显示带六点手柄的临时排序行，使用 Pointer Events 让完整行跟随指针并即时腾位，靠近列表边缘时自动滚动；松手只提交一次完整顺序，无变化、`pointercancel`、`Escape` 或退出排序模式都恢复原序且不保存。手柄可聚焦，`ArrowUp` / `ArrowDown` 调整一位，`Home` / `End` 移到首尾；普通供应商选择卡不得常驻拖拽能力。下方只读详情不重复显示已选 chip 里的供应商名和摘要，只保留 Base URL、API Key、模型绑定和并发等字段，详情卡内的 `编辑` / `复制` / `删除` 是当前供应商二级操作，应在详情卡底部居中排列；`复制` 生成一份新草稿，保留 Base URL、模型绑定、并发和已保存 API Key，但编辑器只能显示掩码占位，不把完整 Key 填入可见输入框，方便同一供应商改名、换模型映射或必要时替换 Key 后保存；只有点击 `编辑` 才展开供应商编辑表单。Base URL 必须按用户明确输入保存，只清理尾部斜杠、查询片段和已包含的完整 `/responses`、`/images/generations`、`/images/edits` 端点，不能为自定义根域名隐式补 `/v1`；`OpenAI Images` / `OpenAI Responses` 中转请求统一发送版本化 `iLab-GPT-Conjure/<version>` User-Agent，不把这一正常客户端标识做成普通用户开关。`新建` 属于供应商集合动作，不和当前供应商的编辑/复制/删除混在一起；`删除` 必须二次确认；编辑态必须提供独立的 `保存供应商` 和足够可点击的 `取消编辑`，并隐藏底部全局保存按钮，避免把编辑保存误解为保存整页设置。新增供应商先进入草稿，不保存不进入供应商列表；正在编辑时切换供应商应提示先保存或取消，不能隐式把编辑框内容写回旧供应商。系统设置顶部只保留居中的“系统设置”标题，不在标题下重复解释四个 Tab；Tab 标签本身就是当前页标题，各 Tab 内容区不再重复同名 h3，也不要在 Tab 下方放默认副标题。API / Codex 的默认说明应由内容控件本身承载，只有保存、新建、编辑、删除或错误这类真实反馈才显示在底部操作区按钮左侧；语言设置只显示“即时生效”状态，不走底部保存按钮；存储设置的“保存后重启 WebUI 生效”属于存储路径保存行为说明，也放在底部保存按钮左侧。去掉副标题后，系统设置标题应在标题行内相对弹窗居中，与右侧关闭按钮垂直居中，关闭按钮不参与标题居中计算；弹窗左右内距由面板整体提供，避免表单贴边或靠 4px 滚动补偿制造窄边感。系统设置 Tab 复用滑动分段底板，选中态文字固定为白色，不让黑字压在主色底板上。系统设置弹窗打开时应按当前 Tab 高度在视口中居中定位，并设置最小 30px 边距；打开后采用顶部锚定，标题、关闭按钮和 Tab 栏切换时保持同一顶部坐标，当前 Tab 内容高度只改变弹窗底部坐标，并用短高度过渡缓冲；不要让不同 Tab 触发整块弹窗垂直居中漂移，也不要把初始位置贴近视口顶部。系统设置弹窗高度跟随当前内容自然收缩，仅设置最大高度和内部滚动兜底；不要用固定高度和 `margin-top: auto` 把保存按钮推到底部制造大片空白。API 设置 Tab 默认桌面视口应尽量不出现内部滚动条，只有内容确实溢出时才在 Tab 面板内部滚动，不让弹窗随内容伸缩或带动背景视觉跳动。

系统设置弹窗在大屏保持约 `624px` 宽，仅在小视口内按两侧各 `30px` 收缩；API 供应商选择卡和只读详情均使用两列布局。供应商编辑表单采用两行四项：第一行供应商标识（可选 emoji 图标与名称同组）和并发上限，第二行 Base URL、API Key；桌面端每项标签与输入同行。Emoji 输入作为供应商名称的可选前缀，在同一组合控件内同时显示 `Emoji 图标` 标签与图形示例，不能仅显示“可选”这类缺少对象的占位文案。新建或编辑实际无 Key 的供应商时必须拒绝保存并将焦点返回 API Key；后端已有隐藏 Key 且 Base URL 仍为同源时继续保留。修改协议、域名或端口造成 origin 变化且未输入新 Key 时，保存先显示包含原、新 origin 的危险确认，默认动作返回填写新 Key；用户明确选择“保留原 Key 并保存”后才允许一次性把原 Key 迁移到新 origin，不能静默清空或静默发送。跨 origin 复制其他供应商 Key 继续拒绝。模型绑定紧随核心字段，连接预览在绑定标题下独占全宽摘要条，以可见标签、浅主题底色和左侧主色标记与可编辑输入区分；“添加模型绑定”为带加号、浅主色填充的标准次级按钮，紧邻模型绑定标题而不贴滚动区右边缘，也不沿用输入框的白底描边语言；“中转站模型名称”和“兼容层”在桌面端同一行，兼容层保持次级技术字段的视觉权重；绑定卡底栏用弱分隔线与参数区区分，“追加比例提示”和“设为默认供应商”组成左侧连续设置组，“删除绑定”作为危险操作固定在右侧，三项保持同一行。比例提示是默认关闭的绑定级兼容选项，开启后按任务提交时的界面语言固化到模型提示，不改变 Codex 内置绑定。视口小于约 `620px` 时核心字段和并发标签恢复标签在上、输入在下，绑定卡改为单列。

### User Configuration Backup And Restore

存储路径下方明确说明更改路径不会搬迁旧数据；保存继续提示重启后生效。当前路径与仍然存在的原默认目录不同时，在存储与通知中提供原生可折叠的“查看原默认目录”说明，按字段展示完整、可换行的路径，支持键盘展开和深浅主题。启动后的简短提示引导用户到此检查缺失历史，不自动打开弹窗或切换目录。目录说明只报告已存在的目录，不把目录存在解释为已经完成数据迁移。

系统设置继续只有四个 Tab。“配置备份与恢复”是“存储与通知”底部的子页面入口，不增加第五个 Tab；进入后标题切换为“备份与恢复”，左侧返回按钮取得焦点，原 Tab 与面板隐藏并设为不可交互，右侧关闭按钮保持原位。返回或按 `Escape` 时恢复原 Tab、滚动位置和入口按钮焦点。存在进行中的传输或尚未领取的备份包时，返回或关闭复用现有锚定式网页确认层，不调用浏览器原生确认框。

子页面用共享分段控件切换“备份 / 恢复”。备份默认选择 chip、公用图、模板和系统设置四类，逐类显示项目数、预计原始大小与警告，至少保留一类；敏感的 API Key 选项只随系统设置显示，隐藏时立即清除。备份进度与结果使用同一纵向阅读轴，完成 ZIP 在 24 小时有效期内可重复下载；备份就绪后“下载备份”成为带下载图标的主操作并位于动作组末端，“创建备份”降为次级操作，下载后仍保留主下载动作，键盘顺序与视觉顺序保持一致。恢复使用真实文件输入，按上传、校验、预览、模式选择和结果推进；预览只列出包中存在的类别，并按颜色、提示词片段、公用图、模板和系统设置分别展示备份数量与当前数量。增量模式保留当前内容，替换模式使用危险色与文字共同表达风险：第一次操作只进入独立确认页，不写数据；确认页明确列出“清除当前数量”和“导入备份数量”，用户勾选确认后最终危险按钮才可用。若备份中的任一受保护子类别为空而当前仍有数据，前端与服务端都必须拒绝替换该类别，并提示取消选择或改用增量恢复；恢复流程不能被用作隐式清空入口。返回预览或改变选择会清除确认状态。

该子页面沿用系统设置弹窗的自然高度、最大高度和内部滚动合同。大屏与 `1024×768` 维持约 `624px` 面板，窄屏按两侧 `30px` 收缩并将动作改为单列，不产生横向滚动；原生文件输入、进度、禁用态、焦点环和危险态都使用现有主题 token。动画只用于短状态过渡，并在 `prefers-reduced-motion` 下关闭。服务端恢复完成后刷新受影响的 chip、公用图、模板与设置视图；路径改变只提示重启，不伪装为已即时切换。

外部供应商不再有全局“调用方式”开关，也不在“OpenAI 兼容”后额外显示第二个调用方式字段。每条模型绑定直接显示可用协议：GPT Image 2 为 `OpenAI Images` / `OpenAI Responses`，Nano Banana 为 `Gemini` / `OpenAI Images`；切换具体模型或协议时自动收敛为合法值。Nano Banana 的 `Gemini` 协议可选标准 / `Gemini ImageConfig`，`OpenAI Images` 可选标准 / `T8 / NewAPI` / `OpenRouter`；其他组合只显示标准。协议和兼容层都是绑定级配置，和远端模型名一起保存，自动派生鉴权、路径和请求编码，不能改变同一供应商内其他模型的请求方式。

### Model Families, Parameters And Provider Bindings

多模型改造后，供应商不再只有一组“调用方式 + 图像模型”高级字段；上文涉及单调用方式、单图像模型编辑器的旧描述由本节取代。一个供应商可包含多条模型绑定，每条绑定独立选择规范模型、直接协议和自定义远端模型名。generate / edit 能力由所选规范模型目录决定，界面不提供操作复选项；普通单绑定保存时写入该模型声明的完整合法集合，旧的同型号拆分绑定则保留既有操作路由，不自动合并。协议只呈现用户能判断的 `OpenAI Images`、`OpenAI Responses`、`Gemini`，并按具体模型收敛为合法集合；界面不再出现“OpenAI 兼容”或独立调用方式。鉴权由协议自动派生，界面不能再出现独立的“Bearer 认证”字段。自定义供应商可保存一个可选的彩色 emoji 标识，作为菜单、卡片与排序行共用的纯文本身份提示。Codex Image / Codex Responses 由两个内置 binding 执行，内部 codec、认证实现名或后端兼容适配器不能暴露给普通用户。

左上品牌固定显示 `iLab CONJURE`，不得因模型切换而改写；模型家族控制独立占据品牌与任务搜索之间的一整行，不再借用品牌第二行或下拉菜单。控件为等宽的两段滑动分段控件，按 `GPT / Gemini` 固定顺序显示 OpenAI、Google Gemini 的小型品牌标识和家族短名，约 `30px` 高、`13px` 文字、`16px` 图标；标识位于主 Logo 下方且显著小于主 Logo，和任务卡共用同一资源。外层承载面和当前项都使用无描边、无阴影的实心低饱和品牌色：浅色主题外层稍深、选中面稍浅，深色主题外层稍浅、选中面稍深，以色阶而非轮廓形成层级。当前项继续使用强调文字；禁止叠加第二层轮廓，也禁止实心深色大块、箭头和弹出菜单。窄侧栏依容器宽度自动隐藏文字，仅保留两枚图标；顶部紧凑壳同样只显示图标且不增加外壳高度。完整家族名继续进入 `title` 和无障碍名称，例如 GPT 保留 `GPT Image`，具体型号 `GPT Image 2` 仍只在输出设置、任务与详情中显示。组件支持指针、Tab、Enter / Space 和左右方向键 / Home / End，焦点环必须可见；切换时不得推动产品名、右侧顶栏、改变外壳高度或制造横向滚动。

同一系列的具体模型在输出设置内切换，例如 Nano Banana Pro / 2 / 2 Lite；同一系列存在多个型号时直接展开为无可见标题的按钮组并保留 accessible name，GPT Image 2 这类单型号系列隐藏整个选择行，判断来自目录数量而不是前端写死系列 ID。参数表单由规范模型 manifest 生成，只渲染当前操作可用的控件；隐藏参数不占布局、不参与提交。有限枚举优先复用 Image 2 的分段按钮：不超过四项使用共享滑动底板，多项比例使用可换行的紧凑按钮网格且不启用滑动底板，唯一可选值显示为只读控件；数量 1–4 同样使用分段按钮，不显示重复的数值回显。高级参数默认收进可展开的“高级设置”，但 manifest 可以声明常开；常开时直接进入主参数流，不再重复显示标题或分隔线。Gemini 不显示输出模态和图片搜索；比例后按“分辨率、数量、安全设置、Google 搜索（若支持）”排列，桌面形成“分辨率｜数量”“安全设置｜Google 搜索”。Lite 没有搜索，安全设置仍占左侧单列，与上方分辨率字段共享左右边界，不能因右侧缺项扩展为整行或另设最大宽度；窄屏按完整字段换行。安全设置不再暴露 JSON，只显示“关闭过滤 / 拦截全部”两个对象预设并继续提交同一个四类别 `gemini.safety_settings` 对象：默认四类显式 `OFF`，拦截全部四类为 `BLOCK_LOW_AND_ABOVE`，旧混合阈值只有在用户主动点击预设后才统一覆盖。Gemini 画面比例使用和 Image 2 同源的 SVG 轮廓与竖横成对网格；首列固定上方 `1:1`、下方 `21:9`，Nano Banana 2 的 `1:4 / 4:1`、`1:8 / 8:1` 各自成列，窄屏按整列换行。搜索控件按具体型号 manifest 显示：Pro 与 Nano Banana 2 只有网页搜索，Lite 不显示。参数较少的非 GPT 模型也要保留稳定的纵向阅读节奏：可编辑动态参数区使用相等上下内距并按紧凑控件组的 `12px` 级节奏排列参数行，字段内部只留轻量呼吸空间；内容从顶部依次成组排列，不使用 `space-between` 等方式均匀拉散；同排字段共享网格轴和上下基线。锁定摘要继承同一自然高度；不为单个模型、单张截图或锁定态单独添加固定高度、最小高度或离散短屏档。主动切换同一系列具体模型时，共同参数按 ID 携带当前合法值；来源值不被目标支持时使用目标默认值，目标独有参数保留其已有合法草稿。当前可编辑参数字段按定义、值和翻译指纹复用未变化 DOM，只替换真实变化字段，复用事件写入当前型号；跨家族仍保留独立草稿，切换供应商不能清空参数。notice 类型参数只承担说明，不得被当成待编码或必填请求参数。

Google 搜索在支持它的 Gemini 型号中使用占满右侧网格单元的“关闭 / 开启”布尔分段控件，与左侧安全设置形成视觉等重的一排；窄屏按字段满宽换行。该选择只改变控件呈现，不改变 `gemini.google_search` 的布尔协议值。Lite 的 manifest 不声明搜索，继续不显示该字段。

“主模型”和“提示词处理”属于 GPT Image 2 的应用级能力，不是通用图像模型参数。Gemini / Nano Banana 只使用当前具体图像模型及其 manifest 参数；无论选择官方接入、Gemini 协议、OpenAI Images 中转，还是当前没有可用供应商，都必须完整隐藏这组 GPT 控件及其外层槽位，不留下空行，也不得提交或持久化 `main_model`、`prompt_fidelity` 和 GPT 提示词约束。GPT Image 2 的 Codex Images / Responses 与外部 `OpenAI Images` / `OpenAI Responses` 绑定均保持既有显示和处理合同。

右上供应商下拉只显示同时支持当前规范模型和当前 generate / edit 操作的供应商。一个中转站可同时出现在多个模型上下文中，但每个上下文使用自己的绑定、协议和远端模型名；长供应商名在按钮和下拉中单行省略，并通过 `title` 与无障碍名称保留全文。没有可用供应商时，选择器内以禁用项目说明并提供进入设置的明确动作，不能在控件下方重复显示溢出的红色提醒，也不能静默选用不兼容供应商。

输出设置继续使用现有网格和自然高度，不为每个模型复制整套面板。动态控件通过 `minmax()`、全宽字段和既有响应式边界重排；参数数量变多时由 dashboard 的既有页面级滚动承接，不能给参数网格另加横向滚动、固定高度或覆盖 Tab。切换家族、具体模型、供应商、历史任务和参数锁定后，都必须保持板块标题、控制列/预览列同底、1024 双栏合同和 390px 单栏无水平溢出。

GPT Image 2 的自定义尺寸卡片只在 `custom` 尺寸模式占位；预设模式及所有非 GPT 模型都必须隐藏卡片、清除自定义网格模式，并让折叠态的最小和最大高度同时归零。不能让模型切换的通用显隐逻辑重新暴露折叠卡片或留下 175px 空白行。

锁定状态选择任意历史任务时，输出设置继续使用原有图形化只读摘要，不切换为参数表单；摘要展示该任务的真实具体型号，Gemini 的 Pro / 2 / Lite 不能省略。短高度摘要压缩覆盖所有宽度，不用桌面宽度条件把中等宽度窗口排除；任务说明和“使用此任务参数”保持在同一文档流 footer，不用绝对定位压住详情。未锁定且查看非当前模型历史任务时，输出设置才进入该任务 manifest 的只读参数检查器，展示请求值、上游实际值、兼容迁移和不可迁移原因；未锁定的当前模型任务恢复到当前模型编辑器。历史查看本身不得修改当前家族、当前供应商或参数草稿。只有明确点击“使用此任务参数”才切换到目标规范模型并采纳兼容值；供应商随后按该目标模型上次使用项或默认项解析，不能保留不兼容选择，也不能任意沿用历史快照中的已失效供应商。Gemini 的 thought 图不得进入最终结果槽；来源引用只在有 grounding metadata 时展示，URL 必须经安全归一化并用安全新窗口属性打开。

Codex 是不可回归合同：默认 GPT Image 2、`Codex Image` / `Codex Responses` 两个具名内置绑定、OAuth 读取、参考文件边界、CLI 参数和历史兼容行为保持不变。多模型目录只为 WebUI API 执行增加路由，不能删除、重命名或偷偷转译现有 Codex binding。

### Motion

动效只用于降低突兀感，不做装饰。模式切换、图库分类切换和面板高度变化使用统一模式：外层高度槽 `220ms ease`，内层内容 `opacity 140ms ease` 加 `transform 180ms ease`。

生成中的持续状态统一使用 B「双弧交接」语义：外弧正向、内弧反向，以连续线性旋转表达任务持续推进，不叠加整体呼吸缩放。右侧运行预览保持 `38px`，外弧 `1.6s linear`、内弧 `1.15s linear`；左侧 `48px` 空缩略图使用 `30px` 双弧，图生图叠图中心使用 `24px` 双弧，两者外弧 `1.3s linear`、内弧 `0.95s linear`。动效只改变 `transform` 与 `opacity`，深浅主题复用主色 token；`prefers-reduced-motion` 下停止旋转并保留静态双弧，继续由状态文字表达运行中。

历史页大图灯箱在多图任务中使用非循环的三槽位平铺：当前图完整居中，左右相邻图固定贴住视口边缘且绝大部分位于屏幕外，只露出可点击切片；第一张隐藏左侧边缘，最后一张隐藏右侧边缘，单图任务不保留空槽。边缘图片是唯一可见的图片导航入口，不能再叠加左右箭头形成两套逻辑，也不能首尾循环。点击边缘图后使用约 `320ms` 的 shared-element 位置交换：当前大图保持自身比例缩小并移动到反方向边缘，被点击的边缘图保持自身比例放大并移动到中央；通过临时真实图片层和 FLIP 几何只变换 `transform` 与 `opacity`，禁止卡背、遮片和伪占位。切换后的中央图及左右相邻图必须预加载并解码；目标三槽位要在动画开始前绑定并保持边缘槽可见，让新一侧图片在移动图片下方自然显露，不能把两侧整体淡出后在交接帧恢复；动画副本的边缘透明度必须读取真实槽位计算值。中央动画副本的终点几何必须在目标槽位绑定、解码并完成布局后，直接读取真实中央图片的矩形，不能根据旧图撑开的中央容器推算；动画副本移除前后的位置与宽高差不得超过 `1 CSS px`。中央动画副本必须以最终中央矩形作为布局基准并以单位变换结束，避免其阴影随缩放矩阵变化；与真实中央图重叠的交接帧只能绘制一层阴影。切换期间不能通过 `pointer-events: none` 撤销被点击边缘的 hover 状态，重复导航由运行态锁拦截，避免单侧从高亮硬切回默认明度。动画层要覆盖到真实槽位连续绘制完成后再移除，真实中央槽位接管帧禁用 opacity 过渡。边缘 hover 不能整体向内平移，必须以贴屏边缘为变换原点缩放，避免产生缝隙。当前图只有高于适合页面比例或正在拖移时才隐藏相邻边缘；等于或小于适合页面比例时边缘保持可见，当前图居中且不启用拖移。键盘左右键继续遵循相同的非循环边界；减少动态效果时退化为短交叉淡化。

历史灯箱移出主图时，目标状态的真实边缘图已经在动画层下方可见；移出动画副本必须在接近边缘的过程中平滑淡出到 `0`，由真实边缘图接管。不能让动画副本以与真实边缘图相同的非零透明度落边，否则两张相同图片会发生 alpha 叠加，动画层移除时从双层亮度硬切回单层亮度。

生成页与历史页大图预览复用同一个非循环三槽位灯箱，统一使用贴住视口两侧的相邻图片切片作为唯一可见图片导航，不保留生成页专用的左右箭头或第二套切图状态。两页共用缩放、shared-element 图片交换、空白关闭和快捷键心智。底部使用单层紧凑工具条承载缩小、当前实际像素比例、放大、适合页面和 `100%` 原始尺寸，不新增浮动面板。滚轮、按钮和键盘必须经过同一比例规范化：接近适合比例时吸附到精确适合状态，同时清除拖移偏移；左右边缘图只在当前图高于适合页面比例或拖动时隐藏，等于或小于适合页面比例时保持可见。点击图片、边缘图或预览控件之外的任意灯箱空白应关闭预览，不能只依赖视口最外层的窄小命中区域。打开预览后的快捷键提示只显示约三秒，使用 `← / →` 表达切图、`↑ / ↓` 表达切任务，并提示滚轮缩放；两页都支持 `0` 适合页面和 `1` 原始尺寸。历史页上下键沿用当前可见历史任务顺序和分页边界，生成页上下键沿用侧栏当前可见任务顺序并跳过没有可显示结果的任务；切换任务后保持最接近的图片槽位，不修改任务数据或生成参数。生成页运行中任务追加输出时，灯箱按当前图片 URL 同步三槽位，保持当前图和缩放心智，不因结果列表增长跳回第一张。

新增动效必须支持 `prefers-reduced-motion`。高度变化不要用生硬的立即切换，也不要用过慢动画阻塞操作。

## Implementation Boundaries

设计系统不是单个 CSS 文件或单个脚本文件。可见 UI 规则应落在本文件；工程执行规则落在 `AGENTS.md`；用户可见能力和使用路径落在 `README.md`。

前端样式应先改 `codex_image/webui/static/styles/` 源分片，再由构建生成 `static/styles.css`。分片按 tokens、base、sidebar、tasks、layout、controls、image/gallery、prompt、settings/preview、responsive 等主题维护。新增组件样式要放进最接近的主题分片，只有跨组件基础变量才进入 token 或 base 分片。

新增可见滚动区域必须使用主题滚动条 token。任何组件自己创建独立 `overflow: auto` 或 `overflow: scroll` 容器时，默认都要同时补 `scrollbar-color: var(--scrollbar-thumb) var(--scrollbar-track)` 和对应 `::-webkit-scrollbar-*` 样式，并使用 `--scrollbar-thumb-hover` 处理 hover，避免回落到系统默认灰色滚动条；只有像历史库分栏这类已在组件规则中明确要求弱化边界的区域，才允许把轨道设为透明，同时仍保留主题滑块和 hover 状态。

TypeScript 模块按用户心智和职责拆分：状态模型、序列化、事件绑定、渲染、动作请求、设置面板和运行反馈分开维护。`main.ts`、`bootstrap.ts`、`legacy-bridge.ts` 只承担组装和兼容桥接，不承载具体视觉分支。

后端模块边界也影响体验一致性。图库、最近参考图、提示词片段、颜色、任务输出、任务缩略图、任务摘要索引、队列恢复、账号路由和执行 transport 都应由专门模块提供稳定数据结构；前端不要用临时 `localStorage` 或隐藏字段伪造长期状态。输出图和上传参考图缩略图都是可重建缓存，SQLite 任务索引只存列表摘要，不能替代原始图片和任务 metadata。

测试应按用户可见主题拆分。新增 UI 行为优先补到 static layout、static prompt、static tasks 等对应测试；后端行为补到 gallery、generation、queue、settings、tasks 等主题测试。测试拆分本身是设计质量的一部分，因为它能防止旧的大文件继续吸收所有交互细节。

## Do's and Don'ts

Do:

- 使用本文件 tokens 和现有 CSS 变量扩展 UI。
- 修改样式时优先编辑 `static/styles/` 源分片，并重新生成 `static/styles.css`。
- 新增组件先查找是否已有 `panel`、`field`、`ghost-button`、`primary-button`、`run-button`、`radio-group`、`modal`、`drawer` 等模式。
- 保持工作台密度，让用户首屏能看到真实操作对象。
- 为静态资源行为变更同步更新测试和资源版本号。
- 修改前端后用本地服务器和浏览器检查关键路径。
- 破坏性操作提供明确确认和可理解后果。
- 让自动增强、图库提示词、参考图说明保持可见和可编辑。
- 用真实任务状态驱动文案，避免把局部失败误报成重试、断线或完成。

Don't:

- 不要把工具做成 landing page、hero 页面或营销型分栏。
- 不要使用装饰性渐变球、背景光斑、纯氛围图片或无意义插画。
- 不要嵌套卡片，也不要把页面 section 包成浮动大卡片。
- 不要为了一个新功能引入整套新配色、新圆角或新按钮体系。
- 不要用不可见的隐藏提示词改变用户生成意图。
- 不要让分类切换、模式切换、图片数量变化造成突然跳高或跳低。
- 不要只用颜色表达错误、禁用、成功或危险状态。
- 不要在没有后端持久化依据时把资源管理状态放进 `localStorage`。
- 不要把新 UI 的状态、渲染、请求、样式继续堆进单个大文件。

GPT Image API 版本共用同一组输出参数和编辑布局。供应商模型绑定支持 Image 2、2.5 Flare 与 2.5 Sunburst 及自定义远端映射；已有绑定不自动改名。生成页只为已配置版本提供紧凑下拉选择，不增加 Gemini 式版本按钮行。历史快照保留规范版本与远端模型身份，Codex 内置绑定仍限 Image 2。
